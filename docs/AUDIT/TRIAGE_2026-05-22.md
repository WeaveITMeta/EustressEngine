# Audit Triage — 2026-05-22 (V1.0 stability campaign)

Verify-first triage of all 20 subsystem audits against the **current code**, produced by 7 parallel analysis agents (one per doc-cluster). This is the canonical fix-backlog; fix-agents report their results back into the per-doc audit checklists (01–20) and tick items here.

Buckets: ✅ done · 🔧 code-fixable-now · 🧠 needs-decision (you) · 🌐 needs-external (account/legal/infra). Tiers: V1.0 / V1.1 / V1.2.

> **Method note.** The audit docs predate recent work and are frequently STALE. Agents (and humans) repeated stale claims — so EVERY fix-agent must re-verify against code before changing anything (cite file:line), exactly as we caught the JWT and Properties false-alarms below.

---

## ⚠️ Corrections to stale audit / agent claims (verified this session)

- **Properties-panel persistence — NOT broken.** The panel writer (`slint_ui.rs` `route_property` → `write_atomic` disk TOML, ALWAYS compiled, covers every standard prop) + the file-watcher → `sync_toml_edits_to_fjall` path persists edits. The `#[cfg(feature="toml")]`-gated `write_instance_changes_system` is a *different, legacy* per-frame mirror, not the live path. → ✅ pending one empirical color-edit→reload confirm. **Do NOT dispatch a fix-agent here.**
- **JWT auth — HTTP backend fully enforced.** Every protected handler validates via `extract_token`/`validate_token` and uses `claims.sub`; `projects.rs` adds `owner_id` checks. Only the QUIC-join JWT (`play_server/server.rs` `with_no_client_auth`) remains → V1.1. → mostly ✅.
- **Bliss balance ledger — agent claims "backend won't compile" (`db.rs` lacks `purchase_item`/`get_user_balance`/`has_purchased`).** UNVERIFIED and high-impact. **VERIFY FIRST** — if the build is currently green, those methods exist somewhere and this is a false alarm; if genuinely missing, it is a real critical gap. Do not let a fix-agent touch this until confirmed.

---

## 🔧 V1.0 code-fixable — first fix-batch (small, self-contained, low-risk; grouped by crate for clean parallel worktrees)

| # | Item | File / module | Risk | Eff | Audit |
|---|---|---|---|---|---|
| A1 | Play button: emit `eustress://play/{id}?token` on status==ready (LaunchInfo already in struct) | `web/src/pages/play.rs:60` | low | S | 06 F5 |
| A2 | Downloads page: consume `releases.eustress.dev/latest.json` + platform auto-detect | `web/src/pages/download.rs` | low | S | 06 F6 |
| B1 | Server: graceful SIGTERM drain (replace placeholder) | `crates/server/src/main.rs:357` | low | S | 03 F4 |
| B2 | Server: structured JSON logging (`fmt().json()`) | `crates/server/src/main.rs:192` | low | S | 03/12 |
| B3 | Server: `/metrics` Prometheus text endpoint | `crates/server/src/main.rs` | low | S | 03/12 |
| C1 | Marketplace: rate-limit `purchase_item` (tower middleware, per-IP) | `crates/backend/src/marketplace.rs:185` | low | S | 09 F12 |
| D1 | Telemetry default: per-topic opt-in; `history.*`+`workshop.tool.*` persistent (resolve doc self-contradiction) | `crates/common/src/change_queue.rs` | med | M | 10 |
| D2 | Watchman cooldown: sim-time ticks alongside wall-time | `crates/common/src/simulation/breakpoint.rs:74` | med | S | 11 R3.1 |
| D3 | Crash reporting: real panic hook → log/dialog (Sentry wiring is 🌐) | `crates/engine/src/telemetry.rs:99` | med | S | 11/12 |
| E1 | Client hot-reload: match `.rune`/`.lua`/`.luau`, not only `.md` | `crates/client/src/soul/hot_reload.rs:86` | med | S | 01 F7 |
| E2 | Client: DPI/scale from winit (not hardcoded 1920×1080) | `crates/client/src/main.rs:49` | low | S | 01/02 F15 |
| F1 | MCP file-watcher: parameterize `is_interesting()` ignore list | `crates/mcp-server/src/watcher.rs:81` | low | S | 17 |
| G1 | CI: add `cargo build -p eustress-engine` to PR CI (catch broken main) | `.github/workflows/ci.yml` | low | S | 12 R1.1 |

These touch web / server / backend / common / engine(telemetry) / client / mcp-server / CI — **disjoint from the uncommitted FPS-batch files**, so they merge cleanly.

## 🔧 V1.0 code-fixable — larger (own batch, sequence after first)

- **DataStore script API → WorldDb backend**: route `with_datastore_service` to `eustress_worlddb::DataStoreService` (`engine/src/soul/rune_ecs_module.rs`) — M. [16]
- **Geo planetary foundation**: `WorldPosition` component + `CameraWorldOrigin` + `hybrid_rebase_system` + post-Avian physics rebase (`common/src/orbital/`) — S+L+M. [14] (only if planetary scale is V1.0; else V1.1)
- **Studio ribbon entries** for CAD/mesh-edit ops (currently undiscoverable) (`engine/src/ui/`) — M. [18]
- **Image-to-Code inline Vision** (one-shot in tool handler) (`engine/src/soul/claude_client.rs`) — M. [07 F2]
- **License + AI-consent (`ai=false`) TOML schema** + loaders (enables C8) (`material_loader.rs`,`instance_loader.rs`) — S. [04 F22] (gated on consent-model decision)
- **Marketplace listing-approval gate** (status draft/pending/approved + KYC auto-approve) (`backend/marketplace.rs`) — M. [09 F6]

## 🔧 V1.1 / V1.2 (rollup — fix-agents per subsystem later)

- **04 Assets**: collider/convex-hull gen; texture compression (BC7/ASTC/Basis) bake; LOD pre-bake; mipmap pre-bake; asset lint pass; Draco graceful-fail.
- **03/05 Net+Streaming**: RemoteEvent/RemoteFunction wire format; server chunk dispatcher + client chunk requester/decoder (the real 10M-scale path); `.echk` vs 56-byte format convergence (🧠); download resumption.
- **07 AI**: embedvec ML embedder (candle all-MiniLM) + persistence; physics-grounded inference (law-registry validation pass); Korah (V1.2).
- **08 Identity**: succession activation logic; content-moderation ML weights; user-action audit log.
- **11 Sim**: Avian state serialization → replay/seek; auto-record-on-play; script debugger UI.
- **13 Terrain**: biome paint wiring; water buoyancy→Avian; navmesh (V1.2, 🧠 Recast vs Rust).
- **14 Geo**: time-of-day sun/moon driver.
- **15 Mobile**: Android JNI `.so` link + iOS XCFramework link; world-load (not hardcoded scene); touch input bridge; app-lifecycle pause/resume. (tier debatable — likely V1.1/V1.2)
- **16 Persistence**: per-user save-data manager.
- **17 Plugin**: LSP IDE extensions (vscode-eustress …); MCP `/resources/subscribe` streaming.
- **18 CAD**: bevel + loop-cut half-edge walker; mesh validation/repair; 2D sketch + constraint solver (V1.2); STEP/IGES (V1.2); Fillet/Chamfer (blocked on truck upstream).
- **19 Realism**: V-Cell thermal↔electrochem coupling; SPH GPU compute (V1.2); fracture→Avian (V1.2); Symbolica feature verify.
- **20 Search**: gallery semantic search (depends on embedvec ML).

---

## 🧠 NEEDS-DECISION (you) — several fixes are gated on these

1. **Telemetry retention/persistence policy** (per-topic opt-in default; sampling strategy) — gates D1. [10]
2. **License + AI-consent model** (license field values; `ai` default false) — gates the C8 schema fix. [04/08]
3. **`.echk` v0 vs 56-byte PackedInstance** — one on-disk + one wire, or converge. [04/05, C17]
4. **Planetary coordinates scope** — is WorldPosition/rebase a V1.0 requirement or V1.1? (affects every Transform reader). [14]
5. **OAuth providers** — Discord-first vs Discord+Google+GitHub (each needs app registration). [08/06]
6. **Account recovery/MFA scope** — email recovery + TOTP (table-stakes) + FIDO2? [08]
7. **Succession inactivity threshold** (365 days default?). [08]
8. **CSAM detection vendor** (PhotoDNA vs Thorn/alternatives) — needs legal counsel; ship-gate. [08]
9. **Money-transmission / tax** — Stripe Connect + Stripe Tax legal review (state MTL, EU VAT/MOSS). [09]
10. **Plugin sandbox** — WASM-only vs native-allowed. [17]
11. **Mobile min targets** (iOS 15/16; Android API 33/34) + tier (V1.1 vs V1.2). [15]
12. **Determinism policy** — lock SIMD + CI CRC compare vs accept divergence. [11/03]
13. **Code-signing budget** — EV (~$400/yr) vs OV cert. [12]

## 🌐 NEEDS-EXTERNAL (you) — accounts / legal / infra / prod

- **macOS notarisation** (Apple Developer acct) · **Windows authenticode** (EV/OV cert). [12]
- **`eustress://` OS protocol handler** registration (Win MSI / macOS plist / Linux .desktop) — gates Play-button end-to-end. [01/06, C13]
- **Sentry/GlitchTip** (DSN + CI symbol upload) · **Prometheus+Grafana** · **HashiCorp Vault** · **Consul** ACL/TLS config. [12]
- **Stripe** account + Connect + Tax + webhooks · **Steam IAP** (Steamworks) · **CSAM**: PhotoDNA partnership + NCMEC CyberTipline. [08/09]
- **OAuth app registrations** (Discord/Google/GitHub). [08]
- **R2 bucket + custom CNAME** (`assets.eustress.dev`) · **App Store / Play Console** accounts + signing. [04/15]
- **Symbolica license** review. [19]

---

## ✅ Verified-done highlights (no action)

Boot/runtime + 9 service plugins; modal tools + undo/redo; Fjall WorldDb (authoritative); soul scripting (Rune+Luau); Ed25519 identity (keypair/issuer/verifier/revocation/witness/history); JWT lifecycle (HTTP); QUIC transport + replication scaffold; backend REST CRUD (28 routes); release pipeline (3-platform + latest.json); SimulationClock/watchpoints/breakpoints/Watchman; stream backbone; embedvec HNSW+persistence; CAD extrude/revolve + mesh-edit extrude/inset; terrain heightmap import+export+brushes+LOD+undo; geo WGS84↔ECEF + HybridPosition math; realism law registry + particle ECS + V-Cell equations; procedural texture gen; MCP server + protocol (Subscribe present).

---

## Sequencing

1. **Commit the pending FPS/stutter/LSP batch** (clean base so fix-agent worktrees merge without tangling). Gated on the user's `--release` rebuild confirmation.
2. **Resolve the gating 🧠 decisions** above that block first-batch items (telemetry policy, consent model).
3. **Wave 2 — first fix-batch**: dispatch the 🔧 first-batch agents (grouped by crate, isolated worktrees), each verifying-then-fixing and reporting into its audit doc. Merge sequentially.
4. **Wave 3+**: larger V1.0, then V1.1/V1.2 per subsystem.
5. The 🧠/🌐 items are surfaced to the founder — they are NOT agent-fixable.
