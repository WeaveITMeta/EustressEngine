# Core runtime deps (pinned).
polars==1.12.0
numpy==2.1.0
scikit-learn==1.5.2
pyarrow==17.0.0

# Optional extras (uncomment to enable):
# colour-science==0.4.4   # alternate reference color math (not required)
# pandas==2.2.3           # fallback DataFrame backend if polars is unavailable
